# Required - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<your-32-char-random-key>
API_KEY=<your-random-api-key>

# Required - CSRF protection secrets (CSRF_SECRET must be EXACTLY 32 characters)
CSRF_SECRET=<exactly-32-character-secret-key>
COOKIE_PARSER_SECRET=<your-cookie-parser-secret>

# Required - Generate password hash and put here
ADMIN_PASSWORD_HASH=<bcrypt-hash>

# Required
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Optional
NODE_ENV=production
DOMAIN_URL=yourdomain.com
NTFY_TOPIC=your-topic-id
LOG_LEVEL=info
PORT=3000